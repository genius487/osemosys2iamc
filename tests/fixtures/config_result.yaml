model: OSeMBE v1.0.0
scenario: DIAG-C400-lin-ResidualFossil
region: region: 'iso2_start' #iso2_x, iso3_y, (where x and y are start, end, or a non-negative number), from_file, and other {country name} are the options for how region should be extracted
results:
- iamc_variable: 'Capacity|Electricity'
  capacity: ['^((?!(EL)|(00)).)*$']
  unit: GW
  osemosys_param: TotalCapacityAnnual
